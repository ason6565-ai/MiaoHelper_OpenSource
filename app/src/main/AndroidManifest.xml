<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <application
        android:name=".MiaoApp"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:usesCleartextTraffic="false"
        android:icon="@mipmap/ic_launcher" android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.MiaoHelper">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RuleEditorActivity"
            android:label="本地词库管理"
            android:exported="false" />
        <activity android:name=".AppStyleActivity"
            android:label="按 App 自动切换风格"
            android:exported="false" />
        <activity android:name=".HistoryActivity"
            android:label="翻译历史"
            android:exported="false" />
        <activity android:name=".PersonaGenActivity"
            android:label="AI 生成人设"
            android:exported="false" />
        <activity android:name=".StyleSettingsActivity"
            android:label="风格设置"
            android:exported="false" />
        <activity android:name=".ApiSettingsActivity"
            android:label="API 设置"
            android:exported="false" />
        <activity android:name=".LogActivity"
            android:label="错误日志"
            android:exported="false" />
        <activity android:name=".TextTranslatorActivity"
            android:label="文本翻译"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".NovelGenActivity"
            android:label="小说生成"
            android:exported="false" />
        <service
            android:name=".MiaoService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/service_desc">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>    </application>
</manifest>
